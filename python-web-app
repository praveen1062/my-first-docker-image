FROM ubuntu:latest

WORKDIR /app

COPY requirements.txt .

RUN apt-get update && \
    apt-get insatll -y python3 python3-pip && \
    pip3 install -r requirements.txt

COPY devops ./devops

WORKDIR /app/devops

ENTRYPOINT ["python3"]

CMD ["manage.py", "runserver", "0.0.0.0.8000"]
